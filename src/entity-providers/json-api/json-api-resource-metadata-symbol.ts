/**
 * Unique symbol used to get json api resource metadata from type manager custom data.
 * 
 * @type {unique symbol}
 */
 export const jsonApiResourceMetadataSymbol: unique symbol = Symbol.for('$ESJsonApiResourceMetadata');
 