/**
 * Defines order direction of an entity collection.
 * 
 * @type {OrderDirection}
 */
export enum OrderDirection
{
    /**
     * Entity collection should be returned in ascending order.
     */
    Asc,
    
    /**
     * Entity collection should be returned in descending order.
     */
    Desc
}
